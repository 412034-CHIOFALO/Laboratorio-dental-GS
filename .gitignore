# ── Maven / Spring Boot ────────────────────────────────────
target/
*.class
*.jar
*.war
*.ear
*.log
spring-boot-*.log

# ── IntelliJ IDEA ──────────────────────────────────────────
.idea/
*.iml
*.iws
*.ipr
out/

# ── VS Code ────────────────────────────────────────────────
.vscode/
.vs/

# ── Angular / Node ─────────────────────────────────────────
frontend-app/node_modules/
frontend-app/dist/
frontend-app/.angular/
frontend-app/*.js.map

# ── Variables de entorno / secretos ────────────────────────
.env
*.env.local
backup/rclone-config/
# NOTA: application-prod.properties SE VERSIONA. Solo usa placeholders
# ${VAR:default} (sin secretos); los valores reales van por env/.env en runtime.
# Asi un clone limpio + docker compose up funciona out-of-the-box.

# ── Claves criptográficas (NUNCA commitear) ────────────────
*.p12
*.jks
*.key
*.pem
**/keys/*.p12
**/keys/*.jks

# ── Sistema operativo ──────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Docker volúmenes locales ───────────────────────────────
docker-volumes/

# ── Backup interno del vault Obsidian ──────────────────────
_obsidian-backup/

# ── Claude Code ────────────────────────────────────────────
.claude/
**/.claude/
